name: tabme_app_localizations
description: "A package providing localizations to the Tabme mobile app."
version: 1.0.1
homepage: https://github.com/TabMe-Co/tabme-app-localizations

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  generate: true